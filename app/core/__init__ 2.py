"""
Core application utilities.

The `core` package stores configuration helpers, constants, and shared
utilities that are broadly useful across the backend (for example,
environment variable parsing or logging helpers).
"""

__all__ = ["config"]

