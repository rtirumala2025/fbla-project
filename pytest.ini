[pytest]
minversion = 7.0
testpaths =
    backend/tests
    tests
asyncio_mode = auto
addopts =
    --strict-markers
    --strict-config
    --cov=app
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
    --cov-fail-under=50
markers =
    slow: mark test as slow running

