[pytest]
minversion = 7.0
testpaths =
    tests
asyncio_mode = auto
addopts =
    --strict-markers
    --strict-config
    --cov=app
    --cov=tests/unit
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
    --cov-fail-under=85
markers =
    slow: mark test as slow running

